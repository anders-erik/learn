// app.js

// Importing the myLibrary module
const library = require('./library');

// Using the greet function from the myLibrary module
const greeting = library.greet('John');

// Displaying the result
console.log(greeting);

